# Add a New Summit

1. Create a summit entry in Keystatic (`summits` collection).
2. Upload a hero image to `public/images/summits/hero/{slug}/`.
3. Select organizers from the `people` collection.
4. Set the summit phase (Planning -> Live -> Archived).
5. Update the `summit` singleton to feature the new summit on the homepage.
