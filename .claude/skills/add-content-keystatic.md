# Add Content via Keystatic

1. Start dev server: `npm run dev`.
2. Open Keystatic: http://localhost:4321/keystatic.
3. Create or edit entries in the visual editor.
4. Commit changes to git (local mode) or push via GitHub (production).
