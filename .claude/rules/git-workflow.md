# Git Workflow

- Always run `git pull origin main` before starting any work.
- Pull again if the user indicates remote changes have been made.
- Commit and push only when explicitly requested by the user.
